# Render Blueprint: deploy from a Git repository with a single click.
services:
  - type: web
    name: mlk-khmer-transcriber-langchhely
    runtime: docker
    branch: public-video-transcriber
    # Standard provides 2 GB RAM, suitable for the bundled Whisper Small model.
    plan: standard
    healthCheckPath: /_stcore/health
